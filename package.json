{
  "name": "w3bsauce",
  "packageManager": "yarn@4.5.1",
  "scripts": {
    "dev": "npx lerna run dev --stream",
    "clean": "npx lerna clean --yes",
    "lint": "yarn run eslint --ext .ts,.tsx .",
    "test": "jest",
    "build": "npx lerna run build",
    "prestart": "yarn build",
    "prepack": "yarn build",
    "compile": "npx lerna run compile",
    "startdev": "npx lerna run watch"
  },
  "workspaces": [
    "packages/*",
    "connectors/*",
    "examples/*"
  ],
  "devDependencies": {
    "lerna": "^8.1.9"
  }
}
